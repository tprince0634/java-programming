package Generic2.WildCards2;

public class Documentations {

 /*In Java generics, wildcards are used to denote an unknown type.
  They are represented by the ? symbol.
  Wildcards are particularly useful when you want to specify that a
  method or a class can operate on a generic type, but you don't know or don't want to
  specify the exact type. There are three types of wildcards in Java generics:

1. Unbounded Wildcard (<?>)
2. Upper Bounded Wildcard (<? extends T>)
3. Lower Bounded Wildcard (<? super T>)


Unbounded Wildcard (<?>)
The unbounded wildcard represents an unknown type.
 It can be used when you want to accept any type, but you don't need to know what the type is.

 Upper Bounded Wildcard (<? extends T>)
The upper bounded wildcard represents an unknown type that is a subtype of a specific type.
 It is used when you want to specify that a generic type must be of a certain type or its subclasses.

 Lower Bounded Wildcard (<? super T>)
The lower bounded wildcard represents an unknown type that is a supertype of a specific type.
 It is used when you want to specify that a generic type must be of a certain type or its superclasses.
*/
}
