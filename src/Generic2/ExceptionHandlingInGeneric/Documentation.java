package Generic2.ExceptionHandlingInGeneric;

public class Documentation {
    /**
     *
     * Generic Exceptions in Java
     * In Java, exceptions are objects that represent an error or unexpected condition
     * during the execution of a program. All exceptions in Java are descendants of Throwable, which has two main subclasses:
     *
     * Exception: Represents checked exceptions (those that need to be declared or handled).
     * RuntimeException: Represents unchecked exceptions (those that don’t need to be
     * declared or handled).
     * */
}
