package Stream.Intermediate_Operation;

public class Conclusion {
    /*Intermediate Operations in Java Streams are operations that process or transform the data
    in a stream and return another stream as a result. They are called lazy operations because
    they do not execute immediately when invoked. Instead, they create a pipeline of operations
    that will be executed only when a terminal operation such as forEach(), collect(), or count()
    is called. Intermediate operations are mainly used for filtering, transforming, sorting,
    and manipulating stream data. Common examples include filter(), map(), flatMap(), sorted(),
    distinct(), limit(), skip(), and peek(). Their main advantage is that multiple operations
    can be chained together efficiently before the final result is produced.*/
}

