package Stream.Terminal_Operations;

public class Theory {
    /* Terminal Operations are the operations that produce the final result of a stream pipeline.
    Unlike intermediate operations, terminal operations do not return a stream.
    They trigger the execution of all preceding intermediate operations and either return a value,
    a collection or perform an action. Once a terminal operation is executed, the stream is
    considered consumed and cannot be reused. Common terminal operations include forEach(),
    collect(), count(), reduce(), findFirst(), findAny(), anyMatch(), allMatch(), noneMatch(),
    min(), and max().*/
}
