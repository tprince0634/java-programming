package Collection.Map.HashTable;

public class Documentation {
    //it IS Synchronized
    //slower (Synchronized) than hashmap
    //Legacy class replaced by ConcurrentHashmap
    //no null key no null values
    //only linked list (In case of Collision Also)

    //All method are Synchronized (Including get) so  ConcurrentHashmap aaya

}
