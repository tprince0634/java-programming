package Collection.Map.IdentityHashMap;

public class Documentation {
    /**
     *IdentityHashMap is a specialized implementation of the Map interface in Java that
     * uses reference equality (==) instead of object equality (.equals) for comparing keys.
     * CHECK KREGA REFERENCE KO RATHER THAN CONTENT (liKE hashmap)
     *
     *  This means that two keys are considered equal only if they refer to the same object in memory.
     *
     *
     * Purpose
     * IdentityHashMap is designed for scenarios where object identity matters more than value equality.*/
}
