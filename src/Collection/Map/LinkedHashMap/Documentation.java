package Collection.Map.LinkedHashMap;

/**
 *     LinkedHashMap
 *
 *  LinkedHashMap is a class in Java that stores key-value pairs.
 *  It extends HashMap and maintains the order of entries.
 *
 *
 * 🔹 Ordering:
 * - By default → maintains insertion order
 * - Can also maintain access order (used in caching)
 *
 *
 * 🔹 Constructor:
 * LinkedHashMap(int initialCapacity, float loadFactor, boolean accessOrder)
 *
 * - accessOrder = false → insertion order (default)
 * - accessOrder = true  → access order (LRU behavior)
 *
 *
 */
public class Documentation {
}