package Collection.Map.ImmutableMap;

public class Document {
    /**
     *
     * ImmutableMap in Java
     * An ImmutableMap is a read-only map that cannot be modified once it is created.
     * It is part of the Guava library provided by Google.
     * An immutable map ensures that its contents (keys and values) cannot be changed after
     * the map has been initialized, making it a great choice when you want to guarantee
     * that a map’s state will remain unchanged throughout its lifetime.
     * */
}
