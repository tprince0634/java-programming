package Collection.Queue;

public class Document {

    /*🔥 The Queue is an interface in Java.

   In Java, a Queue is an interface that represents a First-In-First-Out (FIFO) data structure.
Elements are added at the end (rear)
Elements are removed from the front (head)

👉 Interfaces do not have their own implementation.
👉 They only define behaviors (method signatures).*/


    /**
     * A Queue is a linear data structure that follows the First-In-First-Out (FIFO) principle
     * — elements are added at the end and removed from the front.
     *
     * Implementation classes    Type	            Description
     * LinkedList	          FIFO	             Doubly-linked list. Can be used as a Queue, Deque, or List.
     * PriorityQueue	      Priority	         Elements are ordered by natural ordering or comparator. Not strictly FIFO.
     * ArrayDeque	          Deque	             Double-ended queue. Faster than Stack or LinkedList for stack/queue use.
     * ConcurrentLinkedQueue FIFO (concurrent)	 Thread-safe, non-blocking queue for high-concurrency use.
     *
     *
     *     ✅ Interfaces that Extend the Queue Interface in Java:
     *     Child Interface	Parent Interface
     *     Deque<E>	Queue<E>
     *     BlockingQueue<E>	Queue<E>
     *
     *  */



}
