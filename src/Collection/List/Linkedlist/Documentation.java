package Collection.List.Linkedlist;

public class Documentation {
    /**
     *
     * A linked list is a linear data structure where elements, called nodes,
     * are not stored in contiguous memory locations but are linked using pointers.
     * Each node contains data and a reference to the next node, allowing for efficient
     * insertion and deletion operations. Structure of a Linked List.
     *
     * Nodes: The fundamental building blocks of a linked list, each node consists of:
     * data: The value stored in the node.
     * next: A reference (or pointer) to the next node in the sequence.
     *
     * Head and Tail:
     * The head is the first node in the linked list, serving as the entry point.
     * The tail is the last node, which points to null, indicating the end of the list.
     *
     *
     * Types of Linked Lists
     *
     * Singly Linked List:
     * Each node points to the next node only, allowing traversal in one direction.
     *
     * Doubly Linked List:
     * Each node contains two pointers: one to the next node and one to the previous node, enabling traversal in both directions.
     *
     * Circular Linked List:
     * The last node points back to the head, forming a circle.
     * This can be implemented as either singly or doubly linked.
     * Operations on Linked Lists
     *
     */

}
