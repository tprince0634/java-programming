package Exception_Handling.Throws;

public class Documentation {

    /*The throws keyword is used in a method declaration to indicate that the method may throw certain types of exceptions.
It's used to delegate the responsibility of handling exceptions to the caller of the method.
When a method declares that it throws an exception using the throws keyword, it's informing the compiler that the method may throw that type of exception, and the calling code must either catch the exception or declare it to be thrown further up the call stack.
Multiple exceptions can be declared in the throws clause, separated by commas.*/

    //throws is required only for checked exceptions.

}
