package Opps.Abstraction2.Secondway.Interface;

public class Documentation {

/*
   An interface in Java is a blueprint for a class. It defines a set of abstract methods that implementing classes must provide.
     In Java, an interface is a reference type, similar to a class, that can contain only
     abstract methods (methods without a body),   //// implicitly public and abstract (By dafault)
     default methods (methods with a body),
     and static methods.

     It cannot contain instance variables or constructors
     (except for constants, which are implicitly public, static, and final).

      All variables in an interface are:
      public static final by default (i.e., constants).

 */


}
