package Garbagecollection;

public class Documentation {
    /**
     * In Java, Garbage Collection (GC) is the process by which the JVM automatically reclaims memory
     * by destroying objects that are no longer reachable or in use. Java provides an automatic
     * garbage collection mechanism, so you generally don't need to manually manage memory.
     *
     * However, you can manually trigger the garbage collection process using the System.gc() method,
     * although it is just a suggestion to the JVM and does not guarantee that the garbage collection will
     * happen immediately.
     * */
}
