package TypeCasting;

public class Documentation {
    /*

Upcasting (Implicit Casting):
Upcasting involves converting an object of a subclass type to an object of its superclass type.
It is performed automatically by the compiler when assigning a subclass object to a superclass reference.
Upcasting is considered safe because it involves moving from a more specific type to a more general type.

Downcasting (Explicit Casting):
Downcasting involves converting an object of a superclass type to an object of its subclass type.
It requires an explicit cast operator and may result in a ClassCastException if the object being casted is not actually an instance of the target subclass.
Downcasting is considered risky because it involves moving from a more general type to a more specific type, which may lead to runtime errors if not done properly.

In summary, implicit casting (upcasting) is converting from a subclass to a superclass type, while explicit casting (downcasting) is converting from a superclass to a subclass type. Upcasting is safe and implicit, while downcasting requires an explicit cast operator and may result in runtime errors if not done properly.
 */

    /*
    * WE can say implicit walla is a conversion of smaller datatype in to a  Bigger  (size)
    * And Explicit wala Bigger ->  Smaller (Size) */



}
